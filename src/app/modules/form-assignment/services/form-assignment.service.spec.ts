import { FormAssignmentService } from './form-assignment.service';

describe('FormAssignmentService', () => {
  const service: FormAssignmentService = new FormAssignmentService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
