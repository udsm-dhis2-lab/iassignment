import { FormAssignmentTableComponent } from './form-assignment-table.component';

describe('FormAssignmentTableComponent', () => {
  let component: FormAssignmentTableComponent;

  beforeEach(() => {
    component = new FormAssignmentTableComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
