import { CollectionFormService } from './collection-form.service';

describe('CollectionFormService', () => {
  const service: CollectionFormService = new CollectionFormService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
